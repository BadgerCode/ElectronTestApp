# Test Electron App
Uses Electron and Node to create a simple desktop app.


# Setup
Within the root of this project, run:
> `npm install`


# Start the app
Within the root of this project, run:
> `npm start`
